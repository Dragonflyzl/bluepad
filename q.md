## 角色定位
你是一位资深的 Android 与 Flutter 开发专家，擅长跨平台应用重构与 UI 还原。

## 任务背景
- **参考项目**：`./touch/` 目录下是基于 **Kotlin** 开发的 Android 原生应用
- **目标项目**：`./bluepad-f` 目录下是使用 **Flutter** 重写的版本（旨在实现多平台构建）
- **当前问题**：Flutter 重写版本存在功能或 UI 还原度不足的问题

## 核心任务
严格按照 `./touch` 项目的 UI 设计和功能逻辑，对 `./bluepad-f` 目录下的 Flutter 代码进行修改。

### 执行步骤
1. **对比分析**：先检查两个项目的目录结构，对比关键文件（页面、组件、资源文件）
2. **UI 还原**：确保 Flutter 版本的界面布局、样式、交互与 Kotlin 版本完全一致
3. **功能对齐**：逐功能核对，确保业务逻辑、状态管理、数据流与原生版本等价
4. **代码规范**：保持 Flutter 代码整洁，遵循官方最佳实践

## 重要约束
- **先完全模仿**：第一阶段目标是 1:1 还原，不优化、不改逻辑、不添加新功能
- **Bug 修复后置**：明显的 bug 先记录，待完全还原后再统一修复
- **双向确认**：修改前简要说明对应关系（如 "Kotlin 的 X 页面对应 Flutter 的 Y 文件"）

## 输出要求
- 列出对比检查的文件清单
- 说明每一处修改的原因（对应原生版本的哪个部分）
- 标记发现的差异和待修复的 bug（但不立即修复）

请开始分析两个项目目录。


目前测试的结果如下：
1. 目前可以连接mac，ipad。对于安卓手机无法连接；
2. 无法进行切换设备，比如现在连接的是ipad，我再进行连接mac无法切换到mac；
3. 触控板区域，在按压时会出现一个圆球跟随，但是跟随的位置不对，不在手指的正下方，而且圆球的大小太大了，请你根据touch项目的逻辑进行修改


好的，刚才提出的问题除了安卓不能连接外都解决了，安卓连接暂时先不管，在测试的时候发现以下问题，请进行解决：
1. 测试时发现鼠标在动的时候延时过大，请你进行优化；
2. 在使用两值上下滑动的时候，手指松开后，鼠标会突然向右上方移动，请你进行解决；
3. 触控板双指操作只能上下滚动，左右没有用，请你按照正常的双指操作逻辑进行修改；
4. 在触控板界面的快捷键无效，copy、paste、undo都没有作用，只有switch有效；


## 测试环境
- **设备**: RMX2121 (realme 手机)
- **应用版本**: Flutter 重构版 (最新 commit)
- **测试时间**: 2026-03-04
- **前置条件**: 安卓蓝牙连接已建立（连接功能本身暂不考虑）

---
刚进行了测试，下面是测试时发现的问题：
## 问题清单

### 🔴 P0 - 高优先级

#### 问题 1: 鼠标移动延迟过高
| 项目 | 描述 |
|:---|:---|
| **现象** | 触控板手指移动时，鼠标指针跟随有明显延迟，体验卡顿 |
| **触发条件** | 单指在触控板区域滑动控制鼠标移动时 |
| **预期行为** | 鼠标指针实时跟随手指移动，延迟 &lt; 50ms |
| **实际行为** | 延迟明显，估计 200ms+，有拖尾感 |
| **可能原因** | ① 事件发送频率过低 ② 蓝牙 HID 报告间隔过长 ③ Flutter 手势识别节流 |

---

#### 问题 2: 上下滑动后鼠标异常偏移
| 项目 | 描述 |
|:---|:---|
| **现象** | 两指上下滚动操作结束后，手指松开瞬间鼠标指针突然跳向右上方 |
| **触发条件** | 1. 进入触控板界面&lt;br&gt;2. 使用两指上下滑动（滚动操作）&lt;br&gt;3. 手指离开屏幕 |
| **预期行为** | 手指松开后鼠标保持在原位，无异常移动 |
| **实际行为** | 手指松开后，鼠标指针突然向右上方偏移|
| **可能原因** | ① 手势识别器冲突（Pan vs Scale）&lt;br&gt;② 坐标系转换错误&lt;br&gt;③ 手指抬起时误发了移动事件 |

---

### 🟡 P1 - 中优先级

#### 问题 3: 双指左右滚动无效
| 项目 | 描述 |
|:---|:---|
| **现象** | 触控板双指手势仅支持上下滚动，左右滚动无响应 |
| **触发条件** | 双指在触控板水平方向滑动 |
| **预期行为** | 双指左右滑动时触发水平滚动（如浏览网页/文档时的左右滚动）|
| **实际行为** | 仅双指上下滑动有效，左右滑动被忽略或无响应 |
| **参考标准** | 系统原生触控板：双指滑动方向与滚动方向一致（自然滚动）|
| **可能原因** | ① 手势识别器仅监听 vertical delta&lt;br&gt;② HID 报告仅发送 Y 轴数据 |

---

#### 问题 4: 触控板界面快捷键失效
| 项目 | 描述 |
|:---|:---|
| **现象** | 触控板界面底部/顶部快捷键部分功能无响应 |
| **触发条件** | 点击触控板界面的快捷按钮 |
| **功能矩阵** | | 按键 | 状态 | 预期行为 |&lt;br&gt;|:---|:---|:---|&lt;br&gt;| Copy | ❌ 无效 | 发送 Ctrl+C |&lt;br&gt;| Paste | ❌ 无效 | 发送 Ctrl+V |&lt;br&gt;| Undo | ❌ 无效 | 发送 Ctrl+Z |&lt;br&gt;| Switch | ✅ 有效 | 切换设备/模式 |

| **可能原因** | ① 按钮事件未绑定 HID 键盘报告&lt;br&gt;② 仅 Switch 按钮实现了 Platform Channel 调用 |


1. 两指左右滑动功能没有实现，功能类似于在mac或者win中两指左右滑动是左右移动，在安卓、苹果手机和ipad可以达到左右翻页的效果；
2. 在触控板区域，如果三指滑动的话，会出现卡住的效果，触控板区域的小球会被卡住不动；
3. 触控板模块的快捷键还是没有效果，好像快捷键模块的所有快捷键都没有作用，下面是点击快捷键的调试输出：
    I/MIUIInput(16531): [MotionEvent] ViewRootImpl windowName 'com.dragonfly.bluepad/com.example.bluepad.MainActivity', { action=ACTION_DOWN, id[0]=0, pointerCount=1, eventTime=752098961, downTime=752098961, phoneEventTime=21:00:34.646 } moveCount:0
D/VRI[MainActivity](16531): getMiuiFreeformStackInfo mTmpFrames.miuiFreeFormStackInfo: null
I/MIUIInput(16531): [MotionEvent] ViewRootImpl windowName 'com.dragonfly.bluepad/com.example.bluepad.MainActivity', { action=ACTION_UP, id[0]=0, pointerCount=1, eventTime=752099035, downTime=752098961, phoneEventTime=21:00:34.720 } moveCount:0

1. 三指点击仍然会卡住；
2. 智能识别连接设备的功能没有生效，蓝牙连接mac后点击复制快捷键，仍然是ctrl+c
3. 目前的鼠标移动逻辑是触控板移动的越少鼠标移动的越慢，请修改为线性变化；


请你解决以下问题
1. 三指操作还是会卡住，请你删除三指操作的功能；
2. 智能os识别还是无效，目前连接mac系统点击快捷键还是ctrl；
3. 目前空中飞鼠功能无效，请你照搬touch中的飞鼠逻辑进行修改；
辑进行辑进行辑进行





目录下是使用flutter构建的触控板应用，目前存在一些问题需要解决。请你先对触控板应用的目录结构进行分析，并列出关键文件清单。阅读代码逻辑，然后我将向你提供具体的测试结果，并要求你根据这些结果进行代码修改。

下面是测试时出现的问题：
1. 每次打开app的时候都需要位置授权，请你修改为只在第一次打开的时候需要位置授权；
2. 针对触控板模块，在测试时光标移动，单击，左右键都发现问题，三指操作时触控区域的跟随球会卡死不动，请你进行修改，并请按照下面的功能描述对已有的逻辑进行检测和修改：
    1. 鼠标移动 (Cursor Movement)
    触发条件： 单指在屏幕上滑动 (onPanUpdate 或 PointerMoveEvent)。

    数据采集： 提取两次物理刷新帧之间的相对位移 delta(dx, dy)。

    转换逻辑： * 绝对转相对： 鼠标不需要绝对坐标，只需发送相对位移。

    指针加速 (Pointer Acceleration)： 引入基于滑动速度（Velocity）的非线性加速算法。当滑动速度超过阈值时，对 delta 乘以加速系数，以实现在小屏幕上跨越大屏幕的移动。

    HID 输出： 发送 Mouse Report (X轴相对位移, Y轴相对位移)。
    2. 鼠标左键单击 (Left Click)
    触发条件： 单指短促点击 (onTap)。

    状态控制： 接触屏幕时间 < 200ms，且移动距离 < 设定的防抖像素阈值。

    HID 输出： * 报文 1：Left Button DOWN

    报文 2：Left Button UP

   3. 鼠标右键单击 (Right Click)
    触发条件： 双指同时短促点击 (onSecondaryTap 或 自定义双指 onTap)。

    HID 输出：

    报文 1：Right Button DOWN

    报文 2：Right Button UP

   4. 页面滚动 (Scroll / Wheel)
    触发条件： 双指在屏幕上同向滑动 (onScaleUpdate 中固定缩放比例，提取平移 delta，或通过双指 PointerMoveEvent 计算)。

    数据采集： 提取垂直方向 delta(dy) 和 水平方向 delta(dx)。

    转换逻辑： 需要对滑动距离进行降采样（除以一个阻尼系数），避免触发过快导致主机端页面飞速滚动。

    HID 输出： 发送 Mouse Wheel Report (垂直滚动值, 水平滚动值)。

   5. 拖拽 (Drag and Drop)
    触发条件二 (双击拖拽 - 进阶)： 单指双击，但在第二次按下时不松手直接滑动（符合传统触控板习惯）。

    HID 输出：

    触发时：发送 Left Button DOWN (不发送位移)

    滑动时：发送 Left Button DOWN + 位移 X/Y

    松开时：发送 Left Button UP
3. 针对键盘模块，请按照常用的键盘布局进行修改，分为两种模式：win模式和mac模式，每种模式下快捷键的映射不同；要有键盘的所有功能，包括但不限于：字母区域、数字区域、符号区域、方向键、功能键等。要可以切换模式，快捷键映射要和mac和win系统保持一致；并且可以切换大小写。
4. 针对快捷键模块，目前有很大的bug，快捷键功能可用，但是快捷键无法进行修改和添加，修改为长按快捷键可以进行修改和删除快捷键，点击+可以添加快捷键，注意：添加快捷键时要可选择win模式和mac模式的快捷键，并且快捷键要可以自定义名称；并设置一些常用的图标。



---

# 软件测试问题报告（细化版）

---

## Bug #1 — 位置权限每次启动均重复请求

**问题描述：**
应用每次冷启动时都会弹出位置授权弹窗，用户体验极差，违反移动端权限管理规范。

**期望行为：**
位置权限应仅在**首次启动**时请求一次。用户授权或拒绝后，结果应持久化存储。后续启动直接读取已存储的授权状态，不再重复弹出授权弹窗。

**修复要点：**
- 在权限请求前，先检查本地是否已存储授权结果（如使用 `SharedPreferences` / `UserDefaults` / `flutter_secure_storage` 等）；
- 仅当本地无授权记录时，才触发系统权限请求弹窗；
- 授权完成后，将结果写入本地持久化存储。

---

## Bug #2 — 触控板模块多处功能异常

**整体问题：** 两指操作上下移动速度过快，左右移动无效；三指操作时，触控区域的"跟随球"（指示点）卡死不动。

请按照以下功能规格对现有逻辑进行**逐项排查与修复**：

---

### 2.1 鼠标移动（Cursor Movement）

| 项目 | 规格 |
|---|---|
| **触发条件** | 单指在触控区域滑动（`onPanUpdate` 或 `PointerMoveEvent`） |
| **数据采集** | 提取相邻两帧之间的相对位移 `delta(dx, dy)`，不使用绝对坐标 |
| **指针加速** | 引入基于滑动速度（Velocity）的非线性加速算法。当速度超过设定阈值时，对 `delta` 乘以加速系数，实现小屏幕操控大屏幕的跨越式移动 |
| **HID 输出** | 发送 `Mouse Report`（X 轴相对位移, Y 轴相对位移） |

**重点排查：** 当前光标移动是否误发了绝对坐标；加速算法是否缺失或系数异常。

---

### 2.2 鼠标左键单击（Left Click）

| 项目 | 规格 |
|---|---|
| **触发条件** | 单指短促点击（`onTap`） |
| **状态控制** | 接触时长 `< 200ms`，且移动距离 `<` 防抖像素阈值（建议 `5px`） |
| **HID 输出** | 报文 1：`Left Button DOWN` → 报文 2：`Left Button UP` |

**重点排查：** DOWN/UP 报文是否均被正确发送；防抖逻辑是否将正常点击误拦截。

---

### 2.3 鼠标右键单击（Right Click）

| 项目 | 规格 |
|---|---|
| **触发条件** | 双指同时短促点击（`onSecondaryTap` 或自定义双指 `onTap`） |
| **HID 输出** | 报文 1：`Right Button DOWN` → 报文 2：`Right Button UP` |

**重点排查：** 双指识别是否与双指滚动产生手势冲突；右键报文是否被正确区分发送。

---

### 2.4 页面滚动（Scroll / Wheel）

| 项目 | 规格 |
|---|---|
| **触发条件** | 双指在触控区域同向滑动（可通过 `onScaleUpdate` 提取平移 `delta`，或通过双指 `PointerMoveEvent` 计算） |
| **数据采集** | 提取垂直方向 `delta(dy)` 和水平方向 `delta(dx)` |
| **转换逻辑** | 对滑动距离进行**降采样**（除以阻尼系数，建议初始值为 `3~5`），防止主机端页面滚动过快 |
| **HID 输出** | 发送 `Mouse Wheel Report`（垂直滚动值, 水平滚动值） |

---

### 2.5 拖拽（Drag and Drop）

**触发方式（双击拖拽，符合传统触控板习惯）：**
单指双击，第二次按下后**不松手直接滑动**，进入拖拽模式。

| 阶段 | HID 输出 |
|---|---|
| 触发拖拽时 | 发送 `Left Button DOWN`（不携带位移） |
| 拖拽滑动中 | 持续发送 `Left Button DOWN` + 位移 `(X, Y)` |
| 手指松开时 | 发送 `Left Button UP` |

---

### 2.6 三指操作 — 跟随球卡死问题

**问题描述：** 三指操作时，触控区域的 UI 跟随球（视觉反馈指示点）位置不更新，卡死在初始位置。

**修复要点：**
- 检查三指手势识别器是否正确响应 `onPointerMove` 事件；
- 确认 UI 跟随球的位置更新逻辑是否绑定在手势回调中；
- 确认多指手势识别器在三指状态下没有被其他手势识别器抢占（手势竞争问题）。

---

## Bug #3 — 键盘模块布局不完整，缺少模式切换

**问题描述：** 现有键盘布局不完整，缺少按键区域，且无 Win / Mac 模式切换。

**期望功能规格：**

### 3.1 键盘布局要求（两种模式均需包含以下区域）

| 区域 | 内容 |
|---|---|
| 字母区 | A-Z，支持大小写切换（Caps Lock / Shift） |
| 数字区 | 0-9 |
| 符号区 | 常用标点与特殊符号（`~!@#$%^&*()-_=+[]{}|;':",.<>?/\`` 等） |
| 功能键区 | F1–F12 |
| 方向键区 | ↑ ↓ ← → |
| 控制键区 | `Esc`、`Tab`、`Caps Lock`、`Backspace`、`Delete`、`Enter`、`Space` |
| 导航键区 | `Home`、`End`、`Page Up`、`Page Down`、`Insert` |

### 3.2 模式切换

- 提供 **Win 模式** 和 **Mac 模式** 切换按钮，切换后快捷键映射实时生效；
- 两种模式下的修饰键映射差异：

| 功能 | Win 模式 | Mac 模式 |
|---|---|---|
| 复制 | `Ctrl + C` | `⌘ + C` |
| 粘贴 | `Ctrl + V` | `⌘ + V` |
| 撤销 | `Ctrl + Z` | `⌘ + Z` |
| 全选 | `Ctrl + A` | `⌘ + A` |
| 截图 | `Win + Shift + S` | `⌘ + Shift + 4` |
| 切换窗口 | `Alt + Tab` | `⌘ + Tab` |
| 关闭窗口 | `Alt + F4` | `⌘ + W` |
| 修饰键标注 | `Ctrl / Alt / Win` | `⌃ Ctrl / ⌥ Option / ⌘ Command` |

### 3.3 大小写切换

- 点击 `Caps Lock` 键切换大小写锁定，按键 UI 应有**高亮激活状态**反馈；
- 按住 `Shift` 时临时切换大小写（松开还原）。

---

## Bug #4 — 快捷键模块无法编辑与添加

**问题描述：** 快捷键功能可以触发，但无法修改、删除或添加快捷键，编辑入口完全缺失。

**期望功能规格：**

### 4.1 快捷键编辑交互

| 操作 | 交互方式 |
|---|---|
| **进入编辑模式** | 长按（建议 `500ms`）任意已有快捷键卡片，进入编辑/删除模式 |
| **删除快捷键** | 编辑模式下，卡片显示删除图标（✕），点击删除并弹出二次确认 |
| **修改快捷键** | 编辑模式下，点击卡片进入编辑弹窗，可重新配置 |
| **添加快捷键** | 点击列表末尾的 **`+`** 按钮，进入添加弹窗 |

### 4.2 添加 / 编辑弹窗内容

弹窗应包含以下配置项：

- **快捷键名称**：可自定义文字名称（如"截图"、"一键锁屏"）；
- **图标选择**：提供常用图标库供选择（见下方图标清单）；
- **系统模式选择**：可选 `Win 模式` 或 `Mac 模式`，决定快捷键映射；
- **按键组合配置**：可视化选择修饰键（Ctrl / Alt / Shift / Win / ⌘ / ⌥）+ 主键。

### 4.3 推荐内置图标清单

| 图标用途 | 图标建议 |
|---|---|
| 截图 | 📷 相机 / 剪刀框 |
| 复制/粘贴 | 📋 剪贴板 |
| 搜索 | 🔍 放大镜 |
| 浏览器 | 🌐 地球 |
| 文件管理器 | 📁 文件夹 |
| 锁屏 | 🔒 锁头 |
| 音量 | 🔊 / 🔇 扬声器 |
| 亮度 | ☀️ 太阳 |
| 任务管理器 | ⚙️ 齿轮 |
| 终端 | `>_` 命令行 |
| 撤销/重做 | ↩ / ↪ |
| 自定义 | ✏️ 铅笔（用于标记用户自定义项） |

---

以上为全部 4 个 Bug 的细化描述，每个问题均包含：**问题定位 → 期望行为 → 修复要点/功能规格**，可直接作为开发任务单使用。





1. 每次点开蓝牙连接界面的时候会跳出来位置信息授权；
2. 键盘模块中，虚拟键盘的切换中英文按钮，有时候得按两到三下才能切换，请你找出原因并进行修正
3. 虚拟键盘中，点击功能键，例如